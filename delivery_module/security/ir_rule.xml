<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Teslimat Belgesi Kuralı -->
        <record id="rule_delivery_document" model="ir.rule">
            <field name="name">Teslimat Belgesi Kuralı</field>
            <field name="model_id" ref="model_delivery_document"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_delivery_user')), (4, ref('group_delivery_manager')), (4, ref('group_delivery_driver'))]"/>
        </record>

        <!-- Teslimat Günü Kuralı -->
        <record id="rule_delivery_day" model="ir.rule">
            <field name="name">Teslimat Günü Kuralı</field>
            <field name="model_id" ref="model_delivery_day"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_delivery_user')), (4, ref('group_delivery_manager')), (4, ref('group_delivery_driver'))]"/>
        </record>

        <!-- Teslimat Aracı Kuralı -->
        <record id="rule_delivery_vehicle" model="ir.rule">
            <field name="name">Teslimat Aracı Kuralı</field>
            <field name="model_id" ref="model_delivery_vehicle"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_delivery_user')), (4, ref('group_delivery_manager')), (4, ref('group_delivery_driver'))]"/>
        </record>

        <!-- Teslimat Fotoğrafı Kuralı -->
        <record id="rule_delivery_photo" model="ir.rule">
            <field name="name">Teslimat Fotoğrafı Kuralı</field>
            <field name="model_id" ref="model_delivery_photo"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_delivery_user')), (4, ref('group_delivery_manager')), (4, ref('group_delivery_driver'))]"/>
        </record>

        <!-- İlçe Kuralı - Tüm kullanıcılar ilçeleri görebilir -->
        <record id="rule_res_city_district" model="ir.rule">
            <field name="name">İlçe Kuralı</field>
            <field name="model_id" ref="model_res_city_district"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_delivery_user')), (4, ref('group_delivery_manager')), (4, ref('group_delivery_driver'))]"/>
        </record>

        <!-- İl Kuralı - Tüm kullanıcılar illeri görebilir -->
        <record id="rule_res_city" model="ir.rule">
            <field name="name">İl Kuralı</field>
            <field name="model_id" ref="model_res_city"/>
            <field name="domain_force">[(1, '=', 1)]</field>
            <field name="groups" eval="[(4, ref('group_delivery_user')), (4, ref('group_delivery_manager')), (4, ref('group_delivery_driver'))]"/>
        </record>
    </data>
</odoo> 